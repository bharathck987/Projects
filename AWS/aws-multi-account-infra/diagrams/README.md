# Architecture Diagram

Add your architecture diagram here as `architecture.png` (or `.svg`).

Quick way to make one: draw.io / diagrams.net (free) or excalidraw.com — show the
3 accounts, VPCs, cross-account role arrows, ALB, and ASG. Export as PNG and drop it
in this folder, then it will render automatically in the main README.
